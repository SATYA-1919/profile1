# Sweching Maio Portfolio

A Netflix-inspired one-page portfolio built with plain HTML, CSS, and JavaScript.

## Run locally

Open `index.html` directly in a browser, or from this folder run:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

## Included assets

- `assets/swetch-profile.jpeg`
- `assets/Sweching_Maio_Resume.pdf`
